// src/MinusButton.jsx

function MinusButton() {
  return (
    <button
      style={{
        border: "none",
        borderRadius: "8px",
        padding: "8px 10px",
        cursor: "pointer",
      }}
    >
      감소
    </button>
  );
}

// 내보내기
export default MinusButton;
