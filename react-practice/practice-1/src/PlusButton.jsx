function PlusButton() {
  return (
    <button
      style={{
        border: "none",
        borderRadius: "8px",
        padding: "8px 10px",
        cursor: "pointer",
      }}
    >
      증가
    </button>
  );
}

export default PlusButton;
